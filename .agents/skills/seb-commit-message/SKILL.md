---
name: seb-commit-message
description: Generate a commit message for the current staged changes or all current working tree changes if no staged changes exist. Use when the user asks for a commit message generated from the exact staged snapshot or all working tree changes while enforcing `.agents/rules/commit-message.md`.
---

Read and follow `.agents/rules/commit-message-generation.md` in `automatic`
scope mode.

Return only the final commit message.
